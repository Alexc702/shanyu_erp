# 032 迁移生产镜像资源路径修复 — 本地验证

## 发布阻断

旧发布 `v2-20260919-f0726c4`（提交 `f0726c49e1b4c8e543b23c0d9895d64f12020f61`）不可部署。
API 镜像 ID：`sha256:ccf1d14f85dabf73a0af0f13001e82966e6ffbe779fc264eb7f625968d563156`。
032 只尝试 `apps/api/assets/main-materials/v1/catalog-0919.json`，但镜像 WORKDIR 为 `/app`，资源实际位于 `/app/assets/main-materials/v1/catalog-0919.json`。

此前的镜像探活、资产文件检查及已迁移候选库 dry-run 结果仍是局部验证事实，但未覆盖从 031 到 032 的生产布局迁移，不能据此放行旧包。
旧发布目录中的 RELEASE-STATUS.md 与 DEPLOY-WITH-CODEX.md 已补充阻断标记，归档、镜像和校验和未重写。

## 最小修复

- 仅修改 032 的基线读取函数，按此前迁移的方式支持源码目录和镜像目录。
- 仅 ENOENT 允许尝试下一路径；JSON 损坏及其他读取错误直接抛出。
- 不改迁移 SQL、目标 UUID、主材内容、报价数据保护或其他页面/功能。
- 新增 `apps/api/test/main-material-migration-assets.spec.ts`，覆盖源码布局、镜像布局、文件缺失、源码 JSON 损坏不得回退、镜像 JSON 损坏，共 5 项。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| 原代码新增回归 | 2 通过 / 3 失败，复现生产路径 ENOENT |
| 修复后路径回归 | 5/5 通过 |
| 主材基线回归 | 3/3 通过 |
| API lint / typecheck | 通过，Node 24.20.0 |
| 受控升级部署模拟 | 5/5 通过 |
| 迁移脚本语法 / git diff --check | 通过 |
| 原镜像无网络、无数据库探针 | 确认 ENOENT |
| 隔离 PostgreSQL + 生产镜像布局 031→032 | 通过 |
| 重复执行迁移 | 通过，迁移总数仍为 32 |

真实 PostgreSQL 使用新建容器 `shanyu-migration032-regression-20260920`，无外网、无主机端口，数据库 `shanyu_migration032_test` 存放在 tmpfs。
先由旧 API 镜像执行 001–031，随后创建合成账号、凭据、项目、已报价 V1、派生草稿 V2 及两条主材选型。
将修复的单个迁移文件只读挂载到旧镜像的 `/app/migrations/032_main_material_catalog_0919.mjs`，不挂载源码资源目录；通过 node-pg-migrate 实际执行 032。
这是修复逻辑的镜像布局验证，不代表已经重建或验证最终新发布镜像。

结果：

- 目标主材库 UUID：`32323232-3232-4232-8232-323232323232`。
- 商品 694 条：ACTIVE 601 / PENDING_DATA 89 / INACTIVE 4。
- 业务内容哈希：`31813242011739c37086356ceae7f558cc73106891115f66a01bc95dff6674d2`。
- 13 张业务表通过 deployment-data-baseline.mjs 逐行核验，报价、价格、数量、选型、优惠及凭据不变。
- 原有 1,390 条历史商品版本记录逐项保持不变。
- 安全更新只做 dry-run，两份历史/派生报价均为 PROTECTED；未执行 apply。
- 初次合成数据误用了不允许 MANUAL 的 TILE 分类，触发现有约束；重新创建独立临时库，改用 SHOWER 合成选型后完整重跑通过，未改业务约束。

验证完成已停止并删除本次专用容器，tmpfs 合成数据随之销毁。日常开发库、既有隔离库及云端数据库未操作。

## 发布前仍需完成

修复尚未提交、推送；未重建最终发布镜像，也未部署。全量 API/Web 测试和浏览器 E2E 本轮未重跑，不将上一版测试结果冒充本轮结果。
需要用户确认新本地提交后，从新提交构建新版本镜像及配置包，再对该最终镜像执行 031→032 回归与其他发布门禁。
禁止覆盖旧镜像标签或让云端继续使用 f0726c4 包；不得在云端临时复制文件、补符号链接来绕过此缺陷。
