# 0920 实施与验证：阶段性结果（2026-09-21）

状态：**部分完成，不能作为全部 0920 验收通过或发布依据。**

## 范围与确认

- 基于当前工作树，HEAD `fa25ec6ff22e893463ada7f868e072be5decbf1d`；保留原有未提交修改。
- 用户已确认最新 Pencil 已验收，以及额外 76 条卫浴品名按当前 Excel 纳入候选。
- 数量计价精度没有新增确认：仅格式化显示；存储与金额计算保持原始精度。
- 未 stage、commit、push、部署；未连接云端或写入原本地业务数据库。
- 未修改源 Excel、Pencil、PDF、已发布基线 JSON 或历史报价。

## 本轮实现

1. 新建客餐厅不再追加阳台重复包管。首次草稿仅移除未选、未填写的同名重复项；两项已有数量时保留并阻断提交，提示确认。不自动合并数量；历史派生草稿不自动修复。
2. 正泰空开更换在新建/首次当前草稿中改为可编辑数量，保留原面积初值；清空后不恢复初值。仅删除正泰和施耐德说明中的“单个电箱配置”。
3. 隔音棉与防水腻子的既有引用链已回归验证，无需改公式。
4. 主材表格补充只读单价，非瓷砖隐藏损耗列；按单位格式化数量，保留底层精度与固定操作列。
5. 浴室柜从当前受控备注生成配置说明，保留四组柜型、核心颜色、整柜价格和单位；FULL/DELTA 发布回归包含该说明。
6. 新候选 34A 使用受控三种类型，UI 类型/颜色选项与服务端白名单校验；组合值沿用选型快照字段保存。旧库没有新类型属性时保持旧校验规则。尚缺候选发布后的完整浏览器与导出验证。
7. 新增只写新目录的候选生成器，读取当前 Excel 和朗格嵌入图片，不修改源文件或数据库。

## 候选数据与资产

目录：`output/0920-candidate-20260921-r2/`（相对于仓库根目录）。

- 总表 SHA-256：`95f5bcc0c88ca825a10af947a25d3b50004d3e0c9cd840f4d72e0b982e6cdacb`
- 朗格 SHA-256：`01a6518ccde6c8f13ed886e8f1467673f032fd2872af10076623e263a90db008`
- 商品 694：ACTIVE 599、PENDING_DATA 89、INACTIVE 6。
- 相对 `catalog-0919.json`：201 条业务差异（120 条浴室柜、5 条朗格、76 条卫浴品名），逐 ID 前后值见 `per-id-and-assets-report.json`。
- 450 个资产 SHA-256/大小及关联校验通过；580 条商品引用图片。
- 34A 主图从 `产品图索引!G10` 的真实 drawing 关系提取，748×666，419741 字节；未裁切或重绘；其余色卡关联保留。
- 缺资料状态和源质量警告保留，不猜测补齐。
- **仅候选，尚未发布**；发布前仍需对目标当前库执行受控预览、资产注册和独立授权。

## 实际测试

环境：Node.js 24.20.0、pnpm 10.29.2。

| 检查 | 最终结果 |
| --- | --- |
| API 全量 | 27 文件，179/179 通过，0 失败、0 跳过 |
| Web 全量 | 9 文件，78/78 通过，0 失败、0 跳过 |
| 安全更新 PostgreSQL 专项 | 显式 `SHANYU_SAFE_UPDATE_DB_TEST=1`，包含在上述 API 结果 |
| lint / typecheck / build | 全部通过 |
| 既有部署回归 | 全部脚本通过，含 5 组 guarded-upgrade 测试与独立容器检查 |
| 1280px 浏览器专项 | 瓷砖选择型号及非瓷砖更换可见；单价只读、非瓷砖无损耗列；3.125 输入后后台仍为 3.1250 |
| git diff --check | 通过 |

数据库仅使用本轮容器 `shanyu-0920-acceptance-0921`，127.0.0.1:55462，tmpfs 库 `shanyu_catalog_safe_test`。浏览器使用独立 3100/3101 服务。验收后均停止，原开发数据库未参与写入。

复现和失败记录：

- 新建重复包管、正泰手动数量以及数量显示测试先失败，修复后通过。
- 新 34A 类型/颜色校验测试先失败，补齐后通过。
- 首次 API 全量因隔离库未准备半包模板出现 5 项失败；补齐该隔离库的测试基线后全量通过。
- 部署测试首次被上游 POSTGRES 环境变量干扰，出现测试库认证失败；不改部署脚本，在独立干净环境重跑后全部通过。
- 浏览器首轮在无数据的空表上断言表头失败；补齐隔离测试数据后通过。脚本在 `shanyu_erp/tmp/0920-ui-check.mjs`，截图在候选目录。

## 尚未完成，不可标为通过

- 项目管理模块卡片重排、独立半包/主材折扣与抹零、设计费全链路。
- 报价 Excel/PDF 项目汇总页。
- 独立选材单的队列、快照绑定、字段白名单、缺色/缺图规则、分页与打印模板。
- 上述新模板 Excel/PDF 内容与视觉验收；完整 0920 浏览器端到端验收。
- 新候选的真实受控发布（本轮未授权，未执行）；34A 候选发布后持久化/再次打开/历史快照/导出的闭环验收。

最新 Pencil 已获用户确认，以上 UI/模板不是等待设计批准，而是尚未实施完成。

## 本轮代码范围

- `apps/api/src/quotation/quotation.service.ts` 与对应服务测试。
- `apps/api/src/main-material/main-material-cabinet-mapping.ts`、`main-material-import-mapping.ts`、`main-material-selection.ts` 与对应测试。
- `apps/web/src/app/projects/[projectId]/quotation/main-materials/main-materials-editor.tsx`。
- `apps/web/src/lib/main-material-view-model.ts` 与测试。
- `scripts/prepare-main-material-0920.mjs`、本报告及输出候选/本地验收文件。

现有仓库的其他 dirty 文件、历史主材同步修复和未跟踪资料不属于本轮新增修改，未覆盖或清理。
