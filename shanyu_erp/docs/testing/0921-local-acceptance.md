# 2026-09-21 本地验收与发布判断

## 结论

本轮重新执行现有全量自动化、隔离 HTTP/浏览器和代表性导出检查；确定问题已修复。**不是全部业务场景验收通过，也不建议立即云端发布**。下述待确认、未覆盖项不能用自动化通过代替。

分支 codex/v2-main-materials-v1，基准 HEAD fa25ec6ff22e893463ada7f868e072be5decbf1d。保留原工作树；本轮没有 stage、commit、push、部署或云端访问。未写日常业务数据库。Node 24.20.0 / pnpm 10.29.2。

## 本轮修复

1. 旧版 NULL 主材优惠的待审批报价：新界面曾显示独立半包/主材优惠，与旧整体计算语义不一致。现单独显示“历史整体优惠方案”、整体折扣/抹零/待审批总额，只读，不转换或改写金额。修改 quotation-actions.tsx；隔离浏览器复测通过。
2. 选材单封面：超长名称/地址仍按固定 40pt 行距绘制，复现文字重叠。现按真实换行高度排版，空间不足延续至新页，确认及签字与页数随之调整；不截断原文、不修改快照。修改 selection-sheet.ts，新增长封面回归。普通样例仍 2 页，长地址样例 3 页，封面及签字页已目视复核。

## 本轮实际执行

| 检查                       | 最终结果                                                          |
| ------------------------ | ------------------------------------------------------------- |
| lint / typecheck / build | 全部通过                                                          |
| API                      | 29 文件、195 通过、0 失败、0 跳过                                        |
| Web                      | 9 文件、78 通过、0 失败、0 跳过                                          |
| PostgreSQL 安全更新专项        | 显式 SHANYU_SAFE_UPDATE_DB_TEST=1，21 项包含在上述 API 总数              |
| 部署回归                     | guarded-upgrade 5 项；其余 6 个部署 shell 测试套件通过（共 7 套件），仅替身及隔离容器    |
| 新库迁移                     | 新建隔离库 001–034 成功，导入半包 V5 测试夹具                                 |
| HTTP 真队列                 | 设计费保存、选材添加、提交、报价 XLSX/PDF、选材单、最近任务恢复通过                        |
| 浏览器                      | 项目页、选材单恢复/翻页/125%缩放/下载入口、1280px 无页面整体溢出通过；旧整体优惠待审批展示通过        |
| 导出内容                     | 报价 PDF 8 页；XLSX 5 sheet；55,990+1,870+6,500=64,360；选材单无价格/成本字段 |
| 视觉                       | XLSX 汇总、PDF 汇总、选材单明细、长地址封面和确认页通过代表样例检查                        |
| git diff --check         | 通过                                                            |

隔离环境：Docker shanyu-0920-acceptance-0921，tmpfs PostgreSQL，55462，库 shanyu_catalog_safe_test；临时 Web/API 3100/3101。没有连接日常 3000/3001 或云端。

真实失败记录：首次 5 个测试因新库缺少半包模板夹具失败，使用隔离 HTTP 导入 V5 后重跑通过；最后一次门禁在受限沙箱下出现 39 个本地端口/数据库权限失败（含 listen EPERM），申请本地测试权限后重跑 195/195 通过。没有通过放宽断言掩盖失败。浏览器兼容测试初次使用老板提交主案审批被正常拒绝，换隔离主案账号后验证成功；两次临时文本断言与实际格式不一致，校正测试定位后通过。浏览器仍有 favicon 404，未改无关图标。

HTTP 导出是在本轮长封面修复前运行；修复后 API 全量重跑，并用当前渲染器重新生成长封面 PDF 检查。不能声称最终渲染器已再次走完该 HTTP 全链路。

## 材料候选

本轮新生成 output/0921-full-acceptance-candidate/，没有覆盖旧候选、源表或发布目录。

- 694 条：ACTIVE 599、PENDING_DATA 89、INACTIVE 6；201 个逐 ID 变更。
- 450 个图片资产、580 个商品图片引用，逐文件大小及 SHA-256 校验通过。
- 34A 原图来自朗格产品图索引 G10，748×666 PNG，419741 字节。
- 总表 SHA-256：95f5bcc0c88ca825a10af947a25d3b50004d3e0c9cd840f4d72e0b982e6cdacb。
- 朗格表 SHA-256：01a6518ccde6c8f13ed886e8f1467673f032fd2872af10076623e263a90db008。
- 逐 ID 报告：output/0921-full-acceptance-candidate/per-id-and-assets-report.json。
- **未发布；运行时仍为既有基线。仅提交源表/生成脚本不会让服务器自动得到 0920 数据。**

## 待确认及未完成项

1. 图片审核：当前选材单所有图片是“产品图待补充”，用户需接受占位。是否批准“资产版本＋SHA-256＋审核人”的审核清单，或本版明确仅接受占位？前者尚待实现和验收。
2. 设计费成本/毛利：收入已计入总价，但无设计费成本定义，现有整体毛利会将其视为无新增成本收入，模块成本分析也未单列设计费。建议单列收入、成本/毛利标为未核算，待用户确认；未擅改财务口径。这是发布前业务阻断项。
3. 0920 候选实际受控发布 → 34A 选型 → 真图片 PDF 完整链路未验收。候选生成与映射测试不能替代该链路；正式发布另需授权。
4. 项目外框面积编辑入口 → 设计费同步的完整浏览器闭环未验收；未扩大范围新增项目资料编辑功能。
5. 选材单新增权限、并发任务复用、失败原快照重试的专门 HTTP 异常路径未完整跑；既有权限/worker 单元回归不等于这项已验收。
6. 没有全画板逐像素比对、全部品类真实图片混排、所有浏览器及实体打印验收；本轮仅代表样例 Chrome/导出检查。旧 tests/browser/access.spec.ts 含旧夹具选择器及硬编码 3001，未指向日常环境运行，不能计为通过。
7. 数量原始存储/计价精度继续旧规则，未擅自变更。

## 提交与部署

精确建议代码检查点清单为同目录 0921-commit-paths.txt（仓库根相对路径）。这不是发布批准；待确认项解决后应重新验收、更新清单，再以最终 commit 构建。

不纳入本次代码提交：.obsidian、缓存/runtime/tmp/output 全目录、字体、无关备份清库脚本及运维变动、历史测试文档改写、旧 Excel 删除、未确认资料。PRD/Pencil/源 Excel 可经用户核对后独立资料提交；不能因此视为候选已发布。

部署说明见 ../operations/0921-test-release-next-steps.md。没有生成新镜像/清单或执行云端命令。
