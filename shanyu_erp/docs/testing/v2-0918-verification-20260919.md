# V2 验收 @0918 核对与本地验证（2026-09-19）

依据：工作区《V2 验收 @0918.pdf》全部 3 页；核对当前工作树，不从旧提交重做。保留开始时全部未提交修改，不提交、不推送、不部署，也未操作日常开发或云端业务数据库。

## 逐项结论

| PDF 条目 | 核对结果 | 本次处理 |
| --- | --- | --- |
| 半包取消税金，Excel/PDF 删除税金汇总行且总价不加税 | 当前工作树已实现；计算、成本分析与导出测试覆盖 | 不重复修改 |
| 主材选型增加单位，位于报价数量与品牌/型号之间 | 未实现 | 新增单位列；未选型号的瓷砖需求显示 M²；分组行跨列由 8 调整为 9，保留固定操作列 |
| 非米/平方米计量的基础数量、报价数量为整数 | 未实现，存在未定义的取整口径 | 暂不改。已询问损耗计算或已有数量出现小数时应向上取整、四舍五入，还是只去掉末尾零；未收到确认，不擅改数量和金额 |
| 两项空开施工说明删除“单个电箱配置” | 当前源表解析与工作树已实现 | 导入解析测试与浏览器验证通过；不回写历史快照 |
| 删除 MAT-CEILING-34CB9DC54076 | 0919 基线与隔离库中均不存在 | 新增明确回归断言，不恢复旧商品 |
| 300H-66 改为排风扇，售价 200、成本 120 | 0919 基线已正确 | 既有断言与真实浏览器验证通过 |
| MAT-SEAM-A179F09722C2 品名改为环氧彩砂 | 新基线品名已正确，但卡片及详情优先展示旧型号“美缝” | 仅针对该 material_id 的旧通用型号，以实际品名展示；不改源表型号、价格或历史快照，其他商品展示不变 |

补充资料差异：当前《本科吊顶_v1.xlsx》仍包含无型号、售价 200、成本 80 的旧排风扇行；300H-66 行是排风扇、200/120。主材权威总表及 0919 基线已删除指定旧 material_id。本次只报告资料残留，不修改源 Excel、不据残留资料重新导入商品。

## 本次修改文件

- `apps/web/src/app/projects/[projectId]/quotation/main-materials/main-materials-editor.tsx`：单位列与指定商品展示。
- `apps/web/src/lib/main-material-view-model.ts`：指定商品的旧通用型号展示规则。
- `apps/web/src/lib/main-material-view-model.test.ts`：改名规则及其他型号不受影响。
- `apps/api/test/main-material-baseline.spec.ts`：删除的吊顶商品与环氧彩砂品名断言。
- 本核对记录。

## 本地验证

运行版本 Node.js 24.20.0 / pnpm 10.29.2。

| 检查 | 结果 |
| --- | --- |
| lint、typecheck | 通过 |
| API 全量 | 26 文件，161 项通过，0 失败、0 跳过 |
| Web 全量 | 9 文件，48 项通过，0 失败、0 跳过 |
| 新增基线断言补跑 | 3 项通过 |
| build | 通过 |
| 既有部署脚本 | 6 组、27 PASS；仅本地替身及隔离容器 |
| Chrome 真实浏览器 | 单位列顺序及台单位、1280px 固定操作、吊顶售价/成本、环氧彩砂卡片/详情、空开说明验证通过；无 pageerror |
| git diff --check | 通过 |

数据库测试使用 `shanyu-baseline-audit-20260919` 容器，127.0.0.1:55449，`shanyu_catalog_safe_test` 隔离库；安全更新 PostgreSQL 专项通过 `SHANYU_SAFE_UPDATE_DB_TEST=1` 显式运行。浏览器使用 3300/3301 隔离 Web/API；只在隔离库合成项目继续编辑并添加测试主材。未对日常 3000/3001 环境执行迁移或发布。

以上通过不代表“整数数量”条目已完成；该项仍需业务确认后实现与补测。既有基线迁移是否在实际环境发布生效也不由本次隔离验证代替。
