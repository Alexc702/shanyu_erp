# 山屿 ERP V1 完整测试结果

> 执行日期：2026-08-30  
> 结论：73/73 个 V1 验收用例通过；本地平台达到用户验收条件。  
> 对应用例：[`v1-acceptance-test-cases.md`](./v1-acceptance-test-cases.md)

## 1. 汇总结论

| 用例组 | 数量 | 结果 | 主要证据 |
| --- | ---: | --- | --- |
| A 环境与数据库 | 5 | 5/5 通过 | 运行态自检、独立临时数据库、迁移回滚、不可变触发器 |
| B 登录、会话与用户 | 6 | 6/6 通过 | 访问控制自动化、五账号浏览器登录 |
| C 项目与空间 | 7 | 7/7 通过 | 项目/空间单元与 E2E、完整 8 空间种子项目 |
| D 主材库与 V3 Excel | 10 | 10/10 通过 | V3 解析、导入/发布、成本脱敏、逐行来源检查 |
| E 半包报价与计算 | 15 | 15/15 通过 | 161 行计算测试、浏览器多选/保存/刷新、并发与防篡改 |
| F 成本与毛利 | 5 | 5/5 通过 | 同快照成本计算、老板接口、负向权限 |
| G 审批、版本、导出、审计 | 14 | 14/14 通过 | 真实浏览器业务闭环、API/service 测试、导出签名、审计页 |
| H 视觉、响应式与边界 | 6 | 6/6 通过 | Pencil 对照、桌面和 390×844 浏览器验收 |
| I 发布质量门禁 | 5 | 5/5 通过 | lint/typecheck/test/build/diff-check |
| **合计** | **73** | **73/73 通过** | **无遗留阻断项** |

## 2. 自动化测试与构建

最终同一轮执行：

```bash
pnpm lint
pnpm typecheck
pnpm test
pnpm build
git diff --check
```

结果：

- ESLint：API/Web 全通过，0 warning。
- TypeScript：contracts/API/Web 全通过。
- API：12 个测试文件、50 条测试全部通过。
- Web：5 个测试文件、19 条测试全部通过。
- NestJS 构建成功；Next.js 生产构建成功，共 11 个静态/动态页面入口完成收集。
- `git diff --check`：通过。

API 自动化覆盖登录会话、用户权限、项目空间、主材库导入/发布、V3 工作簿、计价、成本毛利、提交审批、退回/特批、客户导出及审计查询。Web 自动化覆盖工作台角色文案、报价客户端、V3 多选分组、只显示选中项、取消清量和显示格式。

## 3. 本地运行态

`pnpm check:env:runtime` 结果：17 PASS、0 FAIL、1 SKIP（运行态配置按设计跳过重复质量门禁）。

- Node.js `24.20.0`、pnpm `10.29.2`。
- PostgreSQL/MinIO 容器健康，且只绑定 localhost。
- PostgreSQL 查询、MinIO live、API `/health`、Web 根路由均通过。
- 本地 `.env` 只校验变量名，未打印任何密钥值。

## 4. 数据库迁移与恢复

独立创建 `shanyu_erp_v1_acceptance` 临时数据库后执行：

1. 从零应用迁移 001–009：通过。
2. 执行种子：5 个账号、2 个项目、10 个空间；通过。
3. 迁移 009 `down → up`：通过。
4. 删除临时数据库；当前开发数据库未覆盖。

快照篡改负向测试：

- 修改已审批报价的可空 `gross_margin_rate`：数据库拒绝，提示“已提交报价快照不可修改”。
- 修改已审批报价行 `manual_quantity`：数据库拒绝，提示“已提交报价快照明细不可修改”。

## 5. V3 Excel 逐行对算

结构化校验：

- 8 分区、161 项、销售价 161/161、成本价 161/161、41 个数量公式单元格。
- 分区项数：19 / 45 / 44 / 3 / 27 / 3 / 17 / 3。
- 已知空说明只在行 56、105、167。
- 行 80、82、83、89 的四个 V3 新增项名称、单位、销售价、成本和说明全部匹配。
- “正泰空开更换”成本单价为 `6.0000`。

机械对算使用 Excel 已缓存的人工报价结果作为基准，生产代码不执行公式字符串：

| 项目 | Excel / 系统结果 | 差异 |
| --- | ---: | ---: |
| 161 行数量 | 逐行相同 | 0 |
| 161 行销售金额 | 逐行相同 | 0 |
| 有效行预计成本 | 逐行相同 | 0 |
| 8 分区小计 | 逐区相同 | 0 |
| 直接费 | 113124.7660 | 0.0000 |
| 管理费 | 11312.4766 | 0.0000 |
| 报价总计 | 124437.2426 | 0.0000 |
| 预计成本 | 74922.8484 | 0.0000 |
| 预计毛利 | 38201.9176 | 0.0000 |
| 预计毛利率 | 0.3377 | 0.0000 |

## 6. 真实浏览器业务闭环

在本地真实 Web/API/PostgreSQL 上完成：

1. Alex 登录并打开完整八空间 `云栖名苑（V1验收）`。
2. 验证 12 个报价范围与 161 项映射；V3 新增四项只出现在主卧、次卧、衣帽间。
3. 提交 V1（34 个已选项，含管理费 `¥72,192.45`）。
4. 何总进入审批中心，查看成本后批准。
5. 从批准快照生成最终客户 PDF/XLSX：
   - PDF：52,132 bytes，文件头 `25504446`（`%PDF`）；
   - XLSX：9,444 bytes，文件头 `504b0304`；
   - XLSX 自动化解析确认列为分区、工程项、单位、数量、销售单价、金额、施工说明，不含“成本”。
6. Alex 复制为 V2，新增一条砌墙工程并提交；何总填写原因退回；系统自动生成 V3 草稿。
7. 对比 V1/V2，准确显示已选/未选、数量 `1.00` 和两版 2 位金额，不展示原始布尔值或成本。
8. 老板操作日志显示提交、批准、导出、复制、退回、对比及原因；Alex 直访日志页为 404。

## 7. 五角色与响应式

- owner：全部项目、成本毛利、审批、审计可用。
- alex：仅本人项目；无成本、审批和审计数据。
- mori、pm、finance：工作台明确“V1 仅保留账号与固定角色”；已知项目 URL 重定向，审计页 404。
- 390×844：侧栏收为图标栏；报价头操作按钮正确换行；报价范围横向滚动；表格保持可读和可滚动。
- 桌面和窄屏均保持 Pencil 蓝色设计系统，没有绿色旧主题。

## 8. 自验中发现并闭环的问题

| 问题 | 修复 | 复验 |
| --- | --- | --- |
| 审计 metadata 空值导致数据库错误 | 空元数据统一写 `{}` | 登录及审计页通过 |
| 迁移 007 明细触发器字段引用错误 | 迁移 008 修复 | 新草稿创建与提交通过 |
| 可空毛利率可能绕过旧比较符 | 迁移 009 使用 `IS DISTINCT FROM` | 事务内篡改被拒绝 |
| 已退回页面误显示导出入口 | 仅批准/已替代状态显示 | 返回页无 PDF/XLSX 按钮 |
| 客户 PDF 未展示施工说明 | PDF 行加入来源说明 | PDF 单测与浏览器导出通过 |
| 版本差异显示 true/false 和 4 位金额 | 改为已选/未选、数量/金额展示格式 | 浏览器复验通过 |
| 木作/项目经理/财务复用主案权限文案 | 拆分预留角色工作台 | 三角色浏览器复验通过 |
| 390px 报价操作区裁切 | 保存状态独占一行，按钮自动换行 | 390×844 截图复验通过 |

## 9. 交付边界与下一步

本轮交付是可在本地运行的完整 V1 平台，未执行服务器部署。施工、财务、主材 SKU、木作模板、实际成本和实际利润仍明确不在 V1。下一步部署需单独完成生产 Compose、HTTPS、生产密钥、数据库/对象存储备份、日志轮转和恢复演练。

