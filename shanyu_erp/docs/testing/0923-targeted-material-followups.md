# 0923 小范围主材更新与遗留问题验收

## 范围与实际结果

用户确认后，仅在本地数据库发布小范围 DELTA 主材库：V7 → V8。
新版本 UUID：`62f951ff-5804-4bd6-b426-7fc99de82843`。
商品总数仍为 694；124 条变更：

- 120 条铂屿定制浴室柜颜色记录：四种柜型补配置说明（定制镜柜、对应免漆/烤漆柜体、科勒陶瓷盆按米收费、不含龙头）。
- 朗格移门 21AT：规格补“T型淋浴房”。
- 朗格开门 36B：型号改为开门 36A。
- 朗格开门 39AT、41A：新库设 INACTIVE。

未改价格、成本、单位、颜色、图片关联、其他商品及旧库快照。增量发布曾重新归一化未修改商品的 missingFields；已改为只使用上游已归一化的本次变更，未改商品原样复制，并增加 PostgreSQL 回归。

已有项目预演/执行：2 个项目共 5 个报价版本，全部为历史非编辑版本或派生草稿，PROTECTED=5、UPDATED=0、BLOCKED=0。不把“受保护未评估”伪称为业务兼容；按既有规则不自动换库。新建项目使用 V8，旧项目仍显示原绑定版本。重复执行预览为 0 条变更。

## 相关修复

- 派生草稿仅清理未选中、数量和金额均为空/零、有同名客餐厅项的重复阳台包管道。数据库再次限定相同条件且不得有关联主材；其余行 ID、同分区主材关联与金额保留。已填写重复项不自动删除。
- 半包保存按钮等待保存队列，保存中禁用；保存失败后不能显示“草稿已保存”。
- 不改云端、历史库/报价，不提交、不推送、不部署。保留工作区原有其他修改。

## 本轮验证

环境：Node.js 24.20.0 / pnpm 10.29.2；一次性 PostgreSQL 容器 `shanyu-followups-0923`，127.0.0.1:55468，数据库 `shanyu_catalog_safe_test`。回归测试不指向日常库。

| 验证 | 最终结果 |
| --- | --- |
| API 全量 | 212/212，30 文件通过，0 失败/跳过 |
| 其中隔离 PG 安全更新 | 22/22，显式 SHANYU_SAFE_UPDATE_DB_TEST=1 |
| Web 全量 | 81/81，9 文件通过，0 失败/跳过 |
| lint / typecheck / build | 全部通过 |
| 既有部署测试 | 命令退出 0，仅本地替身与隔离容器 |
| 浏览器保存/包管道 | 通过：延迟、失败、成功重载；派生清理、幂等、历史/其他行/主材关联/金额不变 |
| 浏览器材料 | 通过：新项目绑定新库；21AT/36A 展示；39AT/41A 不可选；浴室柜配置说明显示 |
| 小范围发布 | 隔离库预演、提交、幂等通过；日常本地预演、备份、提交、幂等通过 |
| git diff --check | 通过 |

过程中非最终通过的运行如实记录：新测试准备数据时触发已发布快照不可变保护，修正隔离夹具后通过；一次 HTTP socket hang up 重跑通过；浏览器发布后的新库/可编辑样例干扰全量套件固定基线断言，在隔离库恢复测试基线并保护浏览器样例后全量通过。未为匹配断言修改日常 V8 数据或业务验证。

## 证据与备份

- 仓库根目录 `output/0923-targeted-catalog-update/local-preview.json`：逐 ID/字段差异及项目预演。
- 同目录 `local-applied.json`：实际发布结果，`local-idempotence.json`：重复执行零变更。
- 同目录 `update.cjs`：本地一次性受控 DELTA 工具，强制本地地址、事务锁和预览内容哈希，核验旧库及报价业务字段，失败回滚；不是云端部署脚本。
- 同目录 `browser-*.cjs` 与 `*-verified.png`：隔离浏览器验收脚本和截图。
- 应用目录 `tmp/backups/targeted-catalog-0923/before.dump` / `after.dump`：发布前后完整本地备份，权限 0600，pg_restore --list 可读。
- before SHA-256：`4bca4fec9f758efe10e9b1a3579ae8c1cc115b5953f3d79a9ed1475f4274ea9c`。
- after SHA-256：`d9848c55263213ef47102836a2766fd32f0550bccfa5aad6d1de20a871f4cbca`。

云端尚未发布此数据版本。既有项目若需要看到新版材料，必须另行明确处理受保护版本；本次未强制换库。备份及私有逐项报告不应提交到 Git。
